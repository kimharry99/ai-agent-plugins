---
name: python-rules
description: Authoritative Python coding standards — strict OOP, Google Python Style, test layout, and test patterns. Reference when writing or reviewing Python code.
---

# python-rules

When this skill is referenced (e.g. from `/goal` conditions), treat the rule documents listed below as **hard constraints** for any Python work in the current session. Violations must be fixed before completing the work.

## Active rules

@${CLAUDE_PLUGIN_ROOT}/rules/oop.md
@${CLAUDE_PLUGIN_ROOT}/rules/python-style.md
@${CLAUDE_PLUGIN_ROOT}/rules/test-layout.md
@${CLAUDE_PLUGIN_ROOT}/rules/test-patterns.md
